import styled from "styled-components";
import { Col, Image } from 'antd'

export const WrapperStyleSmail = styled(Image)`
    height: 64px;
    width: 64px;
`

export const WrapperStyleColImage = styled(Col)`
    flex-basic: unset;
    displayL: flex;
`